# Supply-chain policy: only install package versions published at least seven
# days ago. Keep the exact dependency versions in package-lock.json as the
# second layer of reproducibility.
min-release-age=7
