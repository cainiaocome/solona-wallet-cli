# Test fixtures

`disposable-keypair.json` is a deterministic, publicly committed Solana keypair used only by Docker E2E tests. It is intentionally disposable, must never receive funds, and is not a developer or production wallet. Do not add real credentials or funded keys here.
